<?xml version="1.0" encoding="UTF-8" ?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:creativeCommons="http://backend.userland.com/creativeCommonsRssModule" xmlns:rdf="http://www.w3.org/1999/02/22-rdf-syntax-ns#" version="2.0">
  <channel>
    <title>{{ feed.title|escape }}</title>
    {% for link in feed.links %}
      <link>{{ link.href|escape }}</link>
      <atom:link rel="{{ link.rel }}" type="{{ link.type }}" href="{{ link.href|escape }}" title="{{ link.title }}"/>
    {% endfor %}
    <description><![CDATA[{{ feed.description }}]]></description>
    {% if feed.rights %}<copyright>{{ feed.rights }}</copyright>{% endif %}
    <generator>{{ feed.generator }}</generator>
    <language>{{ feed.language }}</language>
    {% if feed.tags %}{% for category in feed.tags %}
      <category>{{ category }}</category>
    {% endfor %}{% endif %}
    {% if feed.image %}<image>
      <url>{{ feed.image.href|escape }}</url>
      <title>{{ feed.image.title }}</title>
      <link>{{ feed.image.link }}</link>
    </image>{% endif %}
    <lastBuildDate>{{ feed.updated }}</lastBuildDate>
    {% if feed.docs %}<docs>{{ feed.docs }}</docs>{% endif %}
    {% if feed.ttl %}<ttl>{{ feed.ttl }}</ttl>{% endif %}
    {% if feed.webmaster %}<webMaster>{{ feed.webmaster }}</webMaster>{% endif %}
    {% if feed.summary %}<itunes:summary>{{ feed.summary }}</itunes:summary>{% endif %}
    {% if feed.summary %}<itunes:subtitle>{{ feed.subtitle }}</itunes:subtitle>{% endif %}
    {% if feed.itunes_keywords %}<itunes:keywords>{{ feed.itunes_keywords }}</itunes:keywords>{% endif %}
    <itunes:author>{{ feed.author }}</itunes:author>
    <itunes:owner>
      <itunes:email>{{ feed.author_detail.email }}</itunes:email>
      <itunes:name>{{ feed.author_detail.name }}</itunes:name>
    </itunes:owner>
    {% if feed.itunes_cats %}
      {% for category in feed.itunes_cats %}
        <itunes:category text="{{ category.name }}" >
          {% if category.terms %}
            {% for term in category.terms %}
              <itunes:category text="{{ term }}" ></itunes:category>
            {% endfor %}
          {% endif %}
        </itunes:category>
      {% endfor %}
    {% endif %}
    {% if feed.image %}<itunes:image href="{{ feed.image.href|escape }}" />{% endif %}
    <itunes:explicit>{{ feed.itunes_explicit }}</itunes:explicit>
    {% if feed.itunes_block %}<itunes:block>{{ feed.itunes_block }}</itunes:block>{% endif %}
    {% for entry in entries %}
      <item>
        <title>{{ entry.title }}</title>
        {% if entry.subtitle %}<itunes:subtitle>{{ entry.subtitle }}</itunes:subtitle>{% endif %}
        <description><![CDATA[{{ entry.content.value }}]]></description>
        <pubDate>{{ entry.updated }}</pubDate>
        {% for link in entry.links %}{% ifequal link.rel 'enclosure' %}<enclosure url="{{ link.href|escape }}" length="{{ link.length }}" type="{{ link.type }}"/>{% endifequal %}
        {% ifnotequal link.rel 'enclosure' %}<link>{{ link.href|escape }}</link>{% endifnotequal %}{% endfor %}
        <guid isPermaLink="{{ entry.guidislink }}">{{ entry.id }}</guid>
        {% if entry.author %}<author>{{ entry.author }}</author>
          <itunes:author>{{ entry.author }}</itunes:author>{% endif %}
        {% if entry.tags %}{% for tag in entry.tags %}<category>{{ tag.term }}</category>{% endfor %}{% endif %}
        <itunes:summary>{{ entry.content.value }}</itunes:summary>
        <itunes:duration>{{ entry.itunes_duration }}</itunes:duration>
        <itunes:explicit>{{ entry.itunes_explicit }}</itunes:explicit>
      </item>{% endfor %}
  </channel>

</rss>

